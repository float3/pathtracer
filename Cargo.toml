[package]
name = "pathtracer"
version = "0.1.0"
edition = "2021"

[features]
default = ["small_rng"]
small_rng = ["rand/small_rng"]
thread_rng = []

[dependencies]
cfg-if = "1.0.0"
num-traits = "0.2.18"
png = "0.17.13"
rand = { version = "0.8.5" }
rayon = "1.10.0"
toml = "0.8.12"

[profile.dev]

[profile.release]

[dev-dependencies]
float-cmp = "0.9.0"
